# Tommy ChatGPT-style menu

The production web UI includes the ChatGPT-style Tommy side menu in:

- `app/page.jsx`
- `app/globals.css`

This file is intentionally inside `web/` so the Vercel project receives a new web-directory change and creates a fresh deployment from `main`.
