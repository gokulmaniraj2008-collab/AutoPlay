# Vercel deployment trigger

The Tommy web menu is now included in `web/app/page.jsx` and `web/app/globals.css`.

This file exists only to trigger a fresh Vercel deployment from `main` after the UI update.
